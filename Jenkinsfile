@Library('my-shared-library@master') _

node
{
    
    stage('Demo') {
        
        readProperties "${GIT_SOURCE_URL}", this
        echo "${env.APP_NAME}"
       
               
        }
        
}
